using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/* POI stands for Point Of Interest, this class represents the UI that
 * we will be using to show to the user some interesting moment of the video. 
 * User should click on it if he thinks what is shown on screen is inappropriate,
 * otherwise let it disappear. */
public class POI_UI : MonoBehaviour
{
	// Start is called before the first frame update
	void Start()
	{
		
	}

	// Update is called once per frame
	void Update()
	{
		
	}
}
